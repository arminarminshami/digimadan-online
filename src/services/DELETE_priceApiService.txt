این فایل باید حذف شود: src/services/priceApiService.js

دلیل: این فایل به یک API خارجی ناشناخته (priceto.day) با یک توکن ساده در
همان فایل متصل می‌شد. این توکن در نسخه‌ی نهایی سایت (که در مرورگر کاربر
اجرا می‌شود) قابل مشاهده بود -- یعنی هرکسی می‌توانست آن را ببیند. این فایل
دیگر در هیچ‌جای پروژه import نمی‌شود (فقط در Home.jsx بود که اصلاح شد)،
پس حذف کامل آن امن است.
